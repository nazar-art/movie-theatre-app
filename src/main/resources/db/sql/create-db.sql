--DROP TABLE users IF EXISTS;

CREATE TABLE user (
  id INTEGER PRIMARY KEY,
  name VARCHAR(30),
  gender VARCHAR(30),
  bithday DATE(30),
  email  VARCHAR(50),
  role  VARCHAR(50),
);

-- todo finish filling all needed tables
